# AdvancedCPP Game
## Compilation
1. install conan and cmake(different depending on distro/os)
2. create conan profile: `conan profile detect`
3. run conan: `conan install . --output-folder=build --build=missing`